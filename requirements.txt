# Full project dependencies (notebooks, modelling, dashboard app).
# For the lightweight API runtime only, see requirements-api.txt.
# Minimum-version ranges keep installs reproducible without pinning to
# releases that may not exist on a given platform.

# Core data stack
pandas>=2.0
numpy>=1.26
scipy>=1.11
pyarrow>=14.0          # read/write the processed .parquet datasets

# Machine learning
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
joblib>=1.3

# Visualisation
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18

# Utilities
requests>=2.31

# Notebook environment
jupyter>=1.0
ipykernel>=6.29

# Web app
streamlit>=1.30
